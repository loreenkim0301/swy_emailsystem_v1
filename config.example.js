// EmailJS 설정 예시 파일
// 실제 사용 시 이 값들을 수정하여 사용하세요

const EMAIL_CONFIG = {
    // EmailJS 대시보드에서 확인할 수 있는 Public Key
    publicKey: 'YOUR_PUBLIC_KEY_HERE',
    
    // EmailJS 서비스 ID
    serviceId: 'YOUR_SERVICE_ID_HERE',
    
    // EmailJS 템플릿 ID
    templateId: 'YOUR_TEMPLATE_ID_HERE',
    
    // 이메일을 받을 주소
    toEmail: 'your-email@example.com'
};

// 사용법:
// 1. 위의 값들을 실제 EmailJS 설정값으로 변경
// 2. 웹페이지에서 설정 입력 폼에 이 값들을 입력
// 3. "설정 저장" 버튼 클릭